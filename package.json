{
  "name": "Debug-Include-Toogler",
  "main": "./lib/debug-include-toogler",
  "version": "0.0.1",
  "description": "This package simply allows the user to add/remove a debugging include at the beginning of the file.",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "debug-include-toogler:toggle"
  },
  "repository": "https://github.com/atom/debug-include-toogler",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
